role: apply
version: v1alpha1
phases:
  - name: install
    steps:
      - id: install-runtime-deps-debian
        apiVersion: deck/v1alpha1
        kind: InstallPackages
        timeout: 20m
        spec:
          packages: [containerd, containernetworking-plugins]
