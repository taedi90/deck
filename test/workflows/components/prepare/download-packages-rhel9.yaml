role: pack
version: v1alpha1
prepare:
  packages:
    - group: runtime-deps-rocky9
      targets:
        - osFamily: rhel
          release: rocky9
          arch: "{{ .vars.arch }}"
      repo:
        type: yum
        generate: true
      backend:
        mode: container
        runtime: "{{ .vars.backendRuntime }}"
        image: rockylinux:9
      items:
        - name: bash
