# Legacy Pack Tool Schemas

These schemas remain for legacy or internal prepare planning paths.

New `role: prepare` workflows should prefer top-level `artifacts` instead of authoring these step kinds directly.
