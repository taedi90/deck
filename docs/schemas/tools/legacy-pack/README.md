# Legacy Pack Tool Schemas

These schemas remain for legacy or internal pack planning paths.

New `role: pack` workflows should prefer top-level `prepare` instead of authoring these step kinds directly.
