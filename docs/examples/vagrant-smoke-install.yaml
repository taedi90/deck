role: apply
version: v1alpha1
phases:
  - name: install
    steps:
      - id: create-smoke-marker
        apiVersion: deck/v1alpha1
        kind: RunCommand
        spec:
          command:
            - bash
            - -lc
            - mkdir -p /tmp/deck/examples && printf '%s\n' ok > /tmp/deck/examples/vagrant-smoke.txt
