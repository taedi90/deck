role: apply
version: v1alpha1
phases:
  - name: install
    steps:
      - id: create-smoke-marker
        apiVersion: deck/v1alpha1
        kind: File
        spec:
          action: install
          path: /tmp/deck/examples/vagrant-smoke.txt
          mode: "0644"
          content: |
            ok
