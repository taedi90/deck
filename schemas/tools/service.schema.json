{
  "$id": "https://deck.local/schemas/tools/service.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Start, stop, enable, disable, restart, or reload local services.",
  "properties": {
    "apiVersion": {
      "const": "deck/v1alpha1",
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "kind": {
      "const": "ManageService"
    },
    "metadata": {
      "additionalProperties": true,
      "type": "object"
    },
    "parallelGroup": {
      "minLength": 1,
      "type": "string"
    },
    "register": {
      "additionalProperties": {
        "type": "string"
      },
      "propertyNames": {
        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
      },
      "type": "object"
    },
    "retry": {
      "minimum": 0,
      "type": "integer"
    },
    "spec": {
      "$id": "https://github.com/Airgap-Castaways/deck/internal/stepspec/manage-service",
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "oneOf": [
        {
          "not": {
            "required": [
              "names"
            ]
          },
          "required": [
            "name"
          ]
        },
        {
          "not": {
            "required": [
              "name"
            ]
          },
          "required": [
            "names"
          ]
        }
      ],
      "properties": {
        "daemonReload": {
          "description": "Run `systemctl daemon-reload` before applying state changes.",
          "examples": [
            "true"
          ],
          "type": "boolean"
        },
        "enabled": {
          "description": "Whether the service should be enabled on boot.",
          "examples": [
            "true"
          ],
          "type": "boolean"
        },
        "ifExists": {
          "description": "Only manage the service if it exists on the host.",
          "examples": [
            "true"
          ],
          "type": "boolean"
        },
        "ignoreMissing": {
          "description": "Suppress errors when the service is not found.",
          "examples": [
            "true"
          ],
          "type": "boolean"
        },
        "name": {
          "description": "Single service name to manage. Use `name` or `names`, not both.",
          "examples": [
            "containerd"
          ],
          "minLength": 1,
          "type": "string"
        },
        "names": {
          "description": "Multiple service names managed in one step. Use `name` or `names`, not both.",
          "examples": [
            "[firewalld,ufw]"
          ],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        },
        "state": {
          "description": "Desired service state.",
          "enum": [
            "unchanged",
            "started",
            "stopped",
            "restarted",
            "reloaded"
          ],
          "examples": [
            "started"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "timeout": {
      "pattern": "^[0-9]+(ms|s|m|h)$",
      "type": "string"
    },
    "when": {
      "type": "string"
    }
  },
  "required": [
    "id",
    "kind",
    "spec"
  ],
  "title": "ManageServiceStep",
  "type": "object",
  "x-deck-visibility": "public"
}
