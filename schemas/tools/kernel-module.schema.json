{
  "$id": "https://deck.local/schemas/tools/kernel-module.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Load and persist kernel modules.",
  "properties": {
    "apiVersion": {
      "const": "deck/v1alpha1",
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "kind": {
      "const": "KernelModule"
    },
    "metadata": {
      "additionalProperties": true,
      "type": "object"
    },
    "parallelGroup": {
      "minLength": 1,
      "type": "string"
    },
    "register": {
      "additionalProperties": {
        "type": "string"
      },
      "propertyNames": {
        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
      },
      "type": "object"
    },
    "retry": {
      "minimum": 0,
      "type": "integer"
    },
    "spec": {
      "$id": "https://github.com/Airgap-Castaways/deck/internal/stepspec/kernel-module",
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "oneOf": [
        {
          "not": {
            "required": [
              "names"
            ]
          },
          "required": [
            "name"
          ]
        },
        {
          "not": {
            "required": [
              "name"
            ]
          },
          "required": [
            "names"
          ]
        }
      ],
      "properties": {
        "load": {
          "default": true,
          "description": "Run `modprobe` to load the module immediately.",
          "examples": [
            "true"
          ],
          "type": "boolean"
        },
        "name": {
          "description": "Single module name to manage. Use `name` or `names`, not both.",
          "examples": [
            "br_netfilter"
          ],
          "minLength": 1,
          "type": "string"
        },
        "names": {
          "description": "Multiple module names managed in one step. Use `name` or `names`, not both.",
          "examples": [
            "[overlay,br_netfilter]"
          ],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        },
        "persist": {
          "default": true,
          "description": "Persist the module under `/etc/modules-load.d/`.",
          "examples": [
            "true"
          ],
          "type": "boolean"
        },
        "persistFile": {
          "description": "Path to the persistence file written when `persist` is true.",
          "examples": [
            "/etc/modules-load.d/k8s.conf"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "timeout": {
      "pattern": "^[0-9]+(ms|s|m|h)$",
      "type": "string"
    },
    "when": {
      "type": "string"
    }
  },
  "required": [
    "id",
    "kind",
    "spec"
  ],
  "title": "KernelModuleStep",
  "type": "object",
  "x-deck-visibility": "public"
}
